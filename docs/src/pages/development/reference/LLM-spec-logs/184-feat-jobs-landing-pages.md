## Desc

We're adding jobs landing pages with filters (by orgs, tags, etc), url, and SEO (title, desc, image, etc).

For example as "Climate Change jobs in France".

The filters must be visibly active and restable by the user.

The main long-term complexity may be in reliably converting Django filters to Algolia state.

### Tasks
- [x] v0 implementation
- [x] drop complexity out of `client:prefetch-from-server` → `runPrefetch.ts`
- [x] drop pytest API-coverage tests.
- [x] `useHeadMeta` 
- [x] drop `set` on `useHeroHeader` -> accept its args as props
- [x] on landing pages Algolia filters:
    - on change -> enable Algolia URL params.
    - on reset -> redirect to the homepage.
- [x] `RedirectOnFiltersReset`.
- [x] rename `isModeSSR` to `env.mode.isSSR` and `env.mode.isClient`.
- [x] rename `JobsLandingPage.og_image_url` to `meta_image_url`
- [x] restore `lazyMount` & mount required `useRefinementList`.
- [x] code review #3
    - [x] removal of the prop `uiStateForLandingPage?: IndexUiState`
- [x] Fix e2e requiring the file `JobsLandingPages.json` file. 

## Relevant-Files

BE:
- `server/neuronhub/apps/jobs/{models,admin,graphql,index}.py` — `JobsLandingPage` (slug+SEO+filters).
- `server/neuronhub/apps/jobs/tests/test_gen.py` + `db_reset_and_partial_reindex.py`, `reset_db_and_gen.py`.
- `_get_jobs_by_alert` — Django mirror of FE translator.

FE — landing page core:
- `client/src/prefetch/JobsLandingPage.ts` — gql doc + `graphql.persisted` reg + exported types.
- `client/src/prefetch/JobsLandingPages.json.d.ts` — module shim so tsgo passes without the gitignored JSON.
- `client/src/prefetch/runPrefetch.ts` — build-time vite-node entry (fetch + fs).
- `client/src/sites/pg/pages/jobs-landing-page/[slug].tsx` — `/:slug`; absorbs legacy `/job-slug` via `<Navigate>`.
- `client/src/sites/pg/pages/jobs-landing-page/landingPageToAlgoliaState.ts` — Django→Algolia translator + `useRegisterLandingPageRefinements` (decouples from `PgFacetPopover` `lazyMount`).
- `client/src/sites/pg/pages/jobs/list/JobList.tsx` — preset wiring + local `RedirectOnFiltersClear` (must live inside `<InstantSearch>`).
- `client/src/sites/pg/components/PgAlgoliaList.tsx` — `initialUiStateForIndex` + stateMapping (clean URL when state ≡ preset).
- `client/src/sites/pg/components/PgHeader.tsx` — `PgHeroHeader` reads `useHeroHeader()`.
- `client/src/sites/pg/components/useHeroHeader.tsx` — hero header Valtio store + location-keyed state.
- `client/src/sites/pg/components/PgAlgoliaFacetsActive.tsx` — testid on active facet tags.
- `client/src/sites/pg/routes.ts` — `/:slug` route.
- `client/src/urls.ts` — `urls.jobs.landingPage`.
- `client/tsconfig.json` — `resolveJsonModule: true`.
- `client/e2e/{ids.ts,jobs-landing-page.spec.ts}`.
- `mise.toml` — `client:prefetch-from-server`; `VITE_SERVER_URL` lifted to top-level `[env]`.
- `client/graphql/prefetch/JobsLandingPages.json` — gitignored; generated by `mise client:prefetch-from-server`; e2e `beforeEach` rewrites it for self-contained tests.

FE — prerender + SEO meta:
- `client/react-router.config.ts` — `prerender` maps JSON slugs.
- `client/src/env.ts` — `env.mode.{isSSR,isClient}` lazy getter wraps `import.meta.env.SSR`.
- `client/src/components/useHeadMeta.tsx` + `client/src/root.tsx` — SEO meta Valtio store; root renders `<headMeta.Hoisted />`.
- `client/src/components/posts/PostDetail/index.tsx` — `useHeadMeta({ title: props.post?.title })`.
- `client/src/graphql/client.ts` — Apollo `ssrMode: env.mode.isSSR`.
- `client/src/sites/pg/siteConfigState.ts` — module-init `client.query` guarded by `env.mode.isClient`.
- `client/src/components/AdminMenuFloatButton.tsx` — `localStorage` guarded by `env.mode.isClient`.

## Exec-Plan

### Open items (carried)
1. Wire `client:prefetch-from-server` into Docker build (sibling to `typegen:server`).
2. Sentry env vars for build pipeline (sourcemap auth token).

Latent: `PgAlgoliaList` mobile+desktop CSS-hide → duplicate testids; replace `useBreakpointValue("md")`.

## Decision-Log

#### v0 implementation
- kept prerender despite CLAUDE.md SSR ban — CTO: prefetch JSON + prerender ≠ unwanted SSR. Future refactors must not strip prerender to "comply" with the SSR ban.

#### drop complexity out of `client:prefetch-from-server`
- prefetch script logic → vite-node module (fetch + fs).

#### props.header → useHeroHeader hook
- self-cleanup via location-keyed state (`state.forPath`):
    - tried: PgLayout reset in render → infinite-loop (render-time mutation + subscription; `networkidle` timed out).
    - settled: writer records `location.pathname`; reader returns defaults on mismatch → SSR module-cache leak + client-nav self-clear w/o Layout reset.
- Valtio over React-native title: native emits 2 `<title>`/`<meta>` (defaults + override) → breaks Playwright strict locator + OG-first crawlers.

#### drop pytest API-coverage tests
- BE pytest = security; API shape coverage = e2e's job.

#### landing page Algolia URL params + reset redirect
- `routeToState({}) → preset` survives empty URL on first load AND reloads.
- equality via `JSON.stringify` — brittle: relies on key-order parity between `landingPageToAlgoliaState` output and Algolia roundtrip.
- `RedirectOnFiltersReset` kept local to `JobList` (child of `<PgAlgoliaList>`) — must live inside `<InstantSearch>` for `useClearRefinements` ctx (refactor trap).
- `wasActiveRef` in `RedirectOnFiltersReset`: skips canRefine=false window before preset is applied.

#### CR locked-in (no code change)
- FE prefetch `print(doc)` does NOT bypass BE whitelist — `PersistedQueryExtension.on_parse` asserts unconditionally; matches via normalized AST. `graphql.persisted()` does real work: gql.tada CLI whitelist + `ResultOf<typeof>` types.

#### CR-2: split prefetch — query / lookup / runner
- split avoids re-export footgun: prior `JobsLandingPages.ts` re-exported types from a module w/ top-level `await runPrefetch()`.

#### restore PgFacetPopover lazyMount
- `<RegisterLandingPageRefinements />` must live inside `<InstantSearch>` ctx (refactor trap).
- rejected: hoisting hooks into `PgFacetAttribute` — unrelated to feature.
- `NhaPosthogProvider`: gated `posthog.init` on `env.mode.isClient` — vite-node prerender stubs `posthog.default` (no `.init`) → SSR threw.

#### tolerate missing prefetch JSON
- Vite eagerly resolves `[slug].tsx` JSON import at react-router-dev startup (not lazy as I first assumed — verified by cold-start e2e run).
- `react-router.config.ts`:
    - top-of-module `existsSync`/`writeFileSync` stub so `[slug].tsx`'s static import resolves at vite startup.
    - own `prerender` switched to `readFileSync` (ESM static imports are hoisted ⇒ can't sit below the stub side-effect).
- `client/src/prefetch/JobsLandingPages.json.d.ts`: `declare module` shim for tsgo (config side-effect doesn't run during type-check).
