Read each top-level doc before its children.

You MUST read these before working on tests:
- `pytest.mdx`
- `Playwright.mdx`

To avoid context rot - use a subagent if you need to search the `db_stubs` files.
