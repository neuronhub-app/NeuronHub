## Pytest

- Use subagents to read `pytest-rich` output: it shows local vars, source code, etc.
- Read the `test_gen.py` classes & methods headers (with `rg` to avoid context rot) before writing tests.

You MUST read `pytest.mdx` before working on the server.
