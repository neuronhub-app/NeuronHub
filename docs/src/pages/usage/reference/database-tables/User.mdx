import { LinkExt } from "@/components/LinkExt";
import { PageLink } from "@/components/PageLink";
import { Term } from "@/components/Term";
import { env } from "@/env";


For `Jobs` the `User` is only used to create admins (`superusers`) in the
<PageLink id="admin-panel"/>.

`User` fields:
- `email`
- `password`
- `first_name`
- `is_superuser`

### Links
- <Term id="backend"/> list: <LinkExt href={`${env.VITE_SERVER_URL}/admin/users/user/`}>/admin/users/user/</LinkExt>
