For e2e results reading:
- You can use `| tail` ~32 lines to read the errors.
- In stdout client/server will also print their errors with stacktraces - you can `rg` errors or `client/src`/`server/neuronhub` files out of it.

Read the debug e2e screenshots only using a subagent. Unless you're working on UI and need visual feedback.
