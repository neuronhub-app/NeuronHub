# .npmrc is required by pnpm - can't be replaced it by yaml.
# ------------------------------------------------------------


# inject-workspace-packages: only in docs/Dockerfile for `pnpm deploy`
# ← hardlinks break Node TS stripping in dev (see #164)
