name: neuronhub

services:
  server:
    image: ghcr.io/neuronhub-app/neuronhub/server:0.2.0.0
    env_file:
      - path: .env
    ports:
      - "8000:8000"

  client:
    image: ghcr.io/neuronhub-app/neuronhub/client:0.2.0.0
    env_file:
      - path: .env
    ports:
      - "3000:3000"
