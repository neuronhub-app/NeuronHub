[install-deps]
    depends = [
        "install-deps:server",
        "install-deps:client",
        "install-deps:playwright",
    ]

["install-deps:server"]
    dir = "server"
    run = "uv sync"

["install-deps:client"]
    run = "pnpm install --frozen-lockfile"

["install-deps:playwright"]
    dir = "client"
    run = [
        "pnpm exec playwright install",
        "pnpm exec playwright install chromium --with-deps",
    ]

["install-deps:client:new"]
    run = "pnpm install"


# Upgrades

[upgrade-deps]
    depends = [
        "upgrade-deps:*",
        "install-deps:playwright",
    ]

["upgrade-deps:server"]
    usage = """
        flag "-d --dry-run" default=#false
    """
    dir = "server"
    run = """
        # uv lock & env.DEPENDENCY_COOLDOWN_DAYS --dry-run={{ usage.dry_run }}

        let days_cooldown = {{ env.DEPENDENCY_COOLDOWN_DAYS }}day;
        let date_cutoff = (date now) - $days_cooldown | format date "%+"

        if ($env.usage_dry_run == "false") {
            uv lock --upgrade $"--exclude-newer=($date_cutoff)"
        } else {
            uv lock --upgrade $"--exclude-newer=($date_cutoff)" --dry-run
        }
    """

["upgrade-deps:client"]
    usage = """
        flag "-d --dry-run" default=#false
    """
    run = [
        # We freeze "graphql", due to "gql.tada" bugs with 16.12,
        # See [[package.json.md]] and [[.local/issues/closed/103-gql-tada-Persisted-Queries.md]]
        """
        # check updates with cooldown={{ env.DEPENDENCY_COOLDOWN_DAYS }} --dry-run={{ usage.dry_run }}

        let dry_run = $env.usage_dry_run | into bool

        let $upgrade_json_flag = if ($dry_run == false) { "--upgrade" } else { "" }

        pnpm exec ncu --packageManager pnpm --reject=graphql --packageFile=*/package.json --cooldown={{ env.DEPENDENCY_COOLDOWN_DAYS }} $upgrade_json_flag

        print "\n---\n"

        if ($dry_run) {
            print "Potential upgrades with cooldown=0:\n"
            pnpm exec ncu --packageManager pnpm --cooldown=0
        } else {
            pnpm install
        }
        """
    ]


["upgrade-deps:playwright"]
    dir = "client"
    run = "pnpm update @playwright/test"
